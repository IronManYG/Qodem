object SqlDelight {

    const val version = "2.0.0"
    const val runtime = "app.cash.sqldelight:runtime:${version}"
    const val androidDriver = "app.cash.sqldelight:android-driver:${version}"
    const val coroutinesExtensions = "app.cash.sqldelight:coroutines-extensions:${version}"
    const val primitiveAdapters = "app.cash.sqldelight:primitive-adapters:${version}"

    const val plugin = "app.cash.sqldelight"
}